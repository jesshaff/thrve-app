
{{!-- Placeholder code to show what this should roughly look like --}}

{{#each users as |user|}}

<div class="row align-center mb-5">
  <div class="col-md-6">
    <!-- Call the helper function with triple brackets to render the span as HTML -->
    <h2>{{{get_emoji}}} {{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/#each}}

<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} Test User</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:test@gmail.com">test@gmail.com</a></h3>
  </div>
</div>
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} Test User</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:test@gmail.com">test@gmail.com</a></h3>
  </div>
</div>
